{
  "private": true,
  "description": "",
  "scripts": {
    "update": "git submodule init; git submodule update; git submodule foreach \"(git checkout master; git pull; cd ..)\"; git pull origin master",
  },
  "repository": {
    "type": "git",
    "url": ""
  }
}

